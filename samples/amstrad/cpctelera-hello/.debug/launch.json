{
  "configurations": [
    {
      "type": "zesarux",
      "request": "launch",
      "name": "Debug (CPCTelera/SDCC)",
      "program": "${workspaceFolder}/obj/hello.bin",
      "mapFile": "${workspaceFolder}/obj/hello.map",
      "loadAddress": "0x4000",
      "zesaruxArgs": ["--machine", "cpc6128"],
      "preLaunchTask": "build"
    }
  ]
}
